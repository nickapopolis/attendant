<nav class="navbar navbar-inverse navbar-fixed-top navbar-transparent" role="navigation">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button id="navbar-menu-toggle" type="button" class="navbar-toggle">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar bar1"></span>
        <span class="icon-bar bar2"></span>
        <span class="icon-bar bar3"></span>
      </button>
      <%= link_to 'ATTENDANT', root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
					<li><a class='btn btn-round btn-sm btn-primary menu-dashboard-button' href='/dashboard'>Dashboard</a></li>
					<li><%= link_to('Log Out', destroy_user_session_path, method: :delete, class: 'btn btn-round btn-sm btn-warning menu-logout-button') %></li>
				<% else %>
					<li><%= link_to('Log In', new_user_session_path, class: 'btn btn-round  btn-sm btn-success menu-login-button') %></li>
	        		<li><%= link_to('Register', new_user_registration_path, class: 'btn btn-round btn-sm  btn-primary') %></li>
				<% end %>
    </div><!-- /.navbar-collapse -->
  </div>
</nav>
